# Bucket 5 posttest (fast gate) result
# Command: python -m pytest tests/ -m "not network and not slow"
# Date: 2026-06-22

# Result: 4 failed, 68 passed, 33 deselected, 24 warnings in ~110s

# Failed (4):
#   tests/test_agent_detective.py::test_noise_injection
#     -> Real BLS false-positive in seeded white noise; user chose to leave red
#        per anti-silent-fallback rule. See reports/bucket5_summary.md §1.
#
#   tests/test_agent_detective.py::test_panel_routing
#     -> Pre-existing test bug: file_uploader mock returns a string instead of
#        an UploadedFile, so the upload branch never executes and the
#        "Analyze Telemetry & Verify Harmonics" button never appears.
#        Out of scope for bucket 5.
#
#   tests/test_ui_flow.py::test_ui_flow
#     -> Pre-existing test bug: uses at.file_uploader (does not exist on
#        AppTest; should be at.get("file_uploader")). Out of scope for
#        bucket 5.
#
#   tests/test_workbench_navigation.py::test_workbench_navigation_persistence
#     -> Same file_uploader mock bug as test_panel_routing.
#        Out of scope for bucket 5.

# Deselected (33): all @network and @slow tests excluded from the fast gate.
# See reports/bucket5_summary.md §1 for the full breakdown.
