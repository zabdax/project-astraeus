# Development convenience mirror of the authoritative dependency
# declaration in pyproject.toml. If you change a dependency, change it
# THERE first and mirror it here; these two files must not contradict.
#
# Phase 0 (PRD v4.1 §2.3): wotan and transitleastsquares were previously
# MISSING from this file entirely, which let their absence silently
# change scientific output (fail-open behaviour). They are now required.
# batman is intentionally ABSENT from the required set: it is GPL-3.0 and
# its adoption is an unresolved user-level licensing decision (PRD
# §13.3). Install it explicitly with `pip install batman` or
# `pip install -e ".[batman]"` to enable high-precision subtraction.
# setuptools provides the distutils shim that transitleastsquares needs
# on Python 3.12 (stdlib distutils removed); mirrors pyproject.toml.

setuptools>=75

numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
astropy==6.1.7
plotly==5.24.1
streamlit==1.41.1
lightkurve>=2.4.0
reportlab>=4.1.0, <4.2.0
kaleido==0.2.1
pandas==2.2.3
requests==2.32.3
emcee==3.1.6
boto3>=1.28.0
wotan>=1.10
transitleastsquares>=1.32
